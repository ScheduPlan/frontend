const user = {
    is_loged_in: true,
    role: "manager",
    name: "Max Mustermann",
    email: "max@muster.de"
}

const roles = {
    admin: "admin",
    manager: "manager",
    assembler: "assembler"
}

export default user;